func pow(num: int, to: int): int {
  if (to > 0) {
    return num * pow(num, to - 1)
  } else {
    return "hi"
  }
}

func fib_2(a: int, b: int, max: int) {
  print(a)
  if (b <= max) {
    let sum = a + b
    fib_2(b, sum, max)
  }
}

func fib(max: int) {
  fib_2(0, 1, max)
}