
func plusone(num: int) -> int {
  return num + 1
}

func main(x: int) -> int {
  func plustwo(num: int) -> int {
    return num + 2
  }

  let rename : func int -> int = plusone

  return plusone(rename(plustwo(x,2)))
}