
func plusone(num: int) -> int {
  return num + 1
}

func main(x: int) -> int {
  func plustwo(num: int) -> int {
    return num + 2
  }

  let renamed : func int -> int = plusone

  return plusone(renamed(plustwo(x)))
}